---
- name: Test pod-security-audit-warn-restricted
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Check rendered Namespace labels
      ansible.builtin.command:
        argv:
          - python3
          - "{{ playbook_dir }}/../pod_security.py"
          - pod-security-audit-warn-restricted
      changed_when: false
